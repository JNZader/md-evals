/** EvalRun page — upload SKILL.md + eval YAML and run. */

export default function EvalRun() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-gray-900">Run Evaluation</h1>
      <p className="mt-2 text-gray-600">
        Upload your SKILL.md and eval YAML to run an evaluation.
      </p>
    </div>
  );
}
